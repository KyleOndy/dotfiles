#!/usr/bin/env bash

# Gets the latest LTS version.

curl -sL --compressed https://www.stackage.org | grep LTS | sed -n 's/.*\(LTS [0-9]*\.[0-9]* for GHC [0-9]*\.[0-9]*\.[0-9]*\).*$/\1/p' | column -t
