[package]
name = "system-gpu"
version = "0.1.0"
edition = "2021"
authors = ["Kyle Ondy <kyle@ondy.org>"]
description = "Fast GPU utilization for tmux status bars"
license = "MIT"

[[bin]]
name = "system-gpu"
path = "src/main.rs"

[dependencies]

[profile.release]
lto = true
strip = true
