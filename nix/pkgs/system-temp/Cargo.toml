[package]
name = "system-temp"
version = "0.1.0"
edition = "2021"
authors = ["Kyle Ondy <kyle@ondy.org>"]
description = "Fast CPU/GPU die temperature for tmux status bars"
license = "MIT"

[[bin]]
name = "system-temp"
path = "src/main.rs"

[dependencies]

[profile.release]
lto = true
strip = true
