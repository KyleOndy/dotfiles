[package]
name = "system-load"
version = "0.1.0"
edition = "2021"
authors = ["Kyle Ondy <kyle@ondy.org>"]
description = "One-minute load average for tmux status bars"
license = "MIT"

[[bin]]
name = "system-load"
path = "src/main.rs"

[dependencies]

[profile.release]
lto = true
strip = true
