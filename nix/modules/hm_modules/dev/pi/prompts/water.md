Add context to an existing seed. Do NOT investigate, just append and resume.

1. Resolve which seed the user is referring to. Check seeds you planted this session. If ambiguous, ask.
2. Capture the new context briefly.
3. Run: `c seed water <id> --context "<new context>"` (add `--ticket <id>` if needed).
4. Acknowledge: "Watered S00X".
5. Resume previous work.
