# nix
result
results/

# direnv
.direnv/

# precommit
/.pre-commit-config.yaml

# mcp-servers-nix symlink (generated by devShell shellHook)
/.mcp.json

# VMs
*.qcow2

# terraform
.terraform.lock.hcl
.terraform/
terraform.tfstate.backup
terraform.tfstate.*.backup

.clj-kondo/
.clojure/
.lsp/

# python
__pycache__/

# rust
target/

.aider*

.nrepl-port

# nix closure comparison reports
*closure*report*.md
*closure*report*.json

.cpcache/

# Decrypted host key (temporary during SD card builds)
nix/hosts/cogsworth/keys/ssh_host_ed25519_key

# Fujifilm camera settings backups (embed the camera serial); RE fixtures only
FUJIFILM_*.DAT
