#!/usr/bin/env bash

# allow us to SSH with a local version controlled ssh_config file

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
ssh_config="${SCRIPT_DIR}/../.ssh_config"

ssh_bin=$(which -a ssh | rg -v "$DOTFILES")

$ssh_bin -F "$ssh_config"  "$@"
