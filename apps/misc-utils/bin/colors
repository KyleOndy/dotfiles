#!/usr/bin/env bash

#show what colors are available
NUM_COLORS=16
if ! [ -z "$1" ]; then
  NUM_COLORS="$1"
fi


for i in $(seq 0 "$NUM_COLORS") ; do
  #printf "\x1b[38;5;${i}mcolour${i}\n"
  printf "\x1b[38;5;%imcolour%i\n" "${i}" "${i}"
done
