move to spaceship prompt
  write a new git module to override default
  worktree support
  in rust

XDG cache

refactor out scripts inline i nix into their own files
  zshrc, nvimrc etc

remove paclages I don't use
  vault
  rest of non-tf hashi

Get rid of duplicate packages
  keep the orginization simple, just lexogrpathical sort

replace all nerdfont asumption in starship with unicode

get power useage into tmux bar

firefox pocket disable didn't work
